<div class="container">
  <p id="notice"><%= notice %></p>

  <div class="row text-center hero-banner">
    <div class="col-md-12">
      <h1>The platform to change</h1>
    </div>
    <div class="col-md-12">
      <button type="button" class="btn btn-primary btn-lg btn-custom">Post a Story</button>
    </div>
  </div>
  <div class="row hero-story">
    <div class="col-md-6">
      <img src="http://placehold.it/600x350" class="img-responsive" />
    </div>
    <div class="col-md-6">
      <h2>Google event announced for Tuesday, September 29th</h2>
      <p>
        Google announced its fall event late Friday, sending out press invitations for an event in San Francisco on Tuesday, September 29th at 9am PT. Of course, as we recently learned with Google, almost...
      </p>
    </div>
  </div>




<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.content %></td>
        <td><%= post.user %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to 'Upvote', upvote_post_path(post), method: :patch %></td>
        <td><%= link_to 'Downvote', downvote_post_path(post), method: :patch %></td>
        <td>Upvotes: <%= post.get_upvotes.size %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>

<% if user_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
  </li>
<% else %>
  <li>
  <%= link_to('Login', new_user_session_path)  %>
  </li>
<% end %>

</div>
